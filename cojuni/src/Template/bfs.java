package Template;

public class bfs {

    /*
    * visit
    * need
    * need = startnode
    * while ( need.isEmpty() ) {
    * var node = need.pop()
    * if !visit.contains(node) {
    *   visit += node
    *   need.add(graph(node))
    * }
    *
    * return visit
    * }
     */
}
